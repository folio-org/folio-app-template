{
  "name": "${project.name}",
  "version": "${project.version}",
  "description": "${project.description}",
  "dependencies": [
    {
      "name": "__DEPENDENCY_APP_NAME__",
      "version": "__^SEMVER__",
      "preRelease": "only"
    }
  ],
  "modules": [
    {
      "name": "__MODULE_NAME__",
      "version": "latest",
      "preRelease": "only"
    }
  ],
  "uiModules": [
    {
      "name": "__UI_MODULE_NAME__",
      "version": "latest",
      "preRelease": "only"
    }
  ]
}